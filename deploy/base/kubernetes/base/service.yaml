apiVersion: v1
kind: Service
metadata:
  name: qeet-id
  namespace: qeet-id
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: qeet-id
  ports:
    - name: http
      port: 80
      targetPort: 4001
      protocol: TCP
