# platform/messaging/kafka

Kafka producer and consumer wrappers.

Planned for multi-tenant event streaming when Qeet ID is deployed as part of the Qeet Group
data platform. Uses the event schemas defined in `platform/events/schemas`.
