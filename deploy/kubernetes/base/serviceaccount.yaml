apiVersion: v1
kind: ServiceAccount
metadata:
  name: qeet-id
  namespace: qeet-id
  annotations: {}
  # For AWS IRSA (IAM Roles for Service Accounts), add:
  # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/qeet-id
automountServiceAccountToken: false
