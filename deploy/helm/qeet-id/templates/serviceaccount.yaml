{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "qeet-id.serviceAccountName" . }}
  labels:
    {{- include "qeet-id.labels" . | nindent 4 }}
{{- end }}
