# platform/observability/dashboards

Grafana dashboard JSON definitions and provisioning config.

Runtime dashboards live in `deploy/observability/grafana/dashboards/`. This package
is reserved for Go code that generates or validates dashboard JSON programmatically.
