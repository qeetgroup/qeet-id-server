apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qeet-id.fullname" . }}
  labels:
    {{- include "qeet-id.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
