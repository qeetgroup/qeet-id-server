// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countDAUWindows = `-- name: CountDAUWindows :one
SELECT
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= date_trunc('day', NOW())) AS dau_now,
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= date_trunc('day', NOW() - INTERVAL '1 day')
                                    AND created_at <  date_trunc('day', NOW())) AS dau_prev
FROM auth.sessions
WHERE tenant_id = $1
`

type CountDAUWindowsRow struct {
	DauNow  int64
	DauPrev int64
}

func (q *Queries) CountDAUWindows(ctx context.Context, tid pgtype.UUID) (CountDAUWindowsRow, error) {
	row := q.db.QueryRow(ctx, countDAUWindows, tid)
	var i CountDAUWindowsRow
	err := row.Scan(&i.DauNow, &i.DauPrev)
	return i, err
}

const countFailedLogins24hWindows = `-- name: CountFailedLogins24hWindows :one
SELECT
    COUNT(*) FILTER (WHERE created_at >= NOW() - INTERVAL '24 hours') AS failed_now,
    COUNT(*) FILTER (WHERE created_at >= NOW() - INTERVAL '48 hours'
                      AND created_at <  NOW() - INTERVAL '24 hours') AS failed_prev
FROM audit.events
WHERE tenant_id = $1 AND action = 'auth.login_failed'
`

type CountFailedLogins24hWindowsRow struct {
	FailedNow  int64
	FailedPrev int64
}

func (q *Queries) CountFailedLogins24hWindows(ctx context.Context, tid pgtype.UUID) (CountFailedLogins24hWindowsRow, error) {
	row := q.db.QueryRow(ctx, countFailedLogins24hWindows, tid)
	var i CountFailedLogins24hWindowsRow
	err := row.Scan(&i.FailedNow, &i.FailedPrev)
	return i, err
}

const countLoginsTodayWindows = `-- name: CountLoginsTodayWindows :one
SELECT
    COUNT(*) FILTER (WHERE created_at >= date_trunc('day', NOW())) AS logins_today,
    COUNT(*) FILTER (WHERE created_at >= date_trunc('day', NOW() - INTERVAL '1 day')
                      AND created_at <  date_trunc('day', NOW())) AS logins_yday
FROM auth.sessions
WHERE tenant_id = $1
`

type CountLoginsTodayWindowsRow struct {
	LoginsToday int64
	LoginsYday  int64
}

func (q *Queries) CountLoginsTodayWindows(ctx context.Context, tid pgtype.UUID) (CountLoginsTodayWindowsRow, error) {
	row := q.db.QueryRow(ctx, countLoginsTodayWindows, tid)
	var i CountLoginsTodayWindowsRow
	err := row.Scan(&i.LoginsToday, &i.LoginsYday)
	return i, err
}

const countMAUWindows = `-- name: CountMAUWindows :one

SELECT
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= NOW() - INTERVAL '30 days') AS mau_now,
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= NOW() - INTERVAL '60 days'
                                       AND created_at <  NOW() - INTERVAL '30 days') AS mau_prev
FROM auth.sessions
WHERE tenant_id = $1
`

type CountMAUWindowsRow struct {
	MauNow  int64
	MauPrev int64
}

// Queries for the analytics domain (admin dashboard KPIs + chart projections).
//
// These are the static aggregations whose result types sqlc can infer cleanly.
// A handful of projections stay hand-written in analytics.go because sqlc cannot
// infer their return types without changing behaviour:
//   - kpis() MFA-adoption + MFA-adoption-prior: the mixed-type CASE
//     (integer 0 vs numeric ratio) resolves to numeric, which sqlc emits as
//     interface{} rather than float64.
//   - extraKPIs() avg-sessions-per-user: COALESCE(CASE ... ::float, 0) also
//     resolves to interface{} under sqlc.
//   - trends14d(): users_cum = COALESCE(bigint) + COALESCE(SUM(...) numeric) is
//     mis-inferred as int32 while the code scans int64 (narrowing/overflow risk).
//   - loginMethodsMix(): COALESCE(metadata->>'method', ...) (jsonb ->>) is
//     emitted as interface{} rather than string.
func (q *Queries) CountMAUWindows(ctx context.Context, tid pgtype.UUID) (CountMAUWindowsRow, error) {
	row := q.db.QueryRow(ctx, countMAUWindows, tid)
	var i CountMAUWindowsRow
	err := row.Scan(&i.MauNow, &i.MauPrev)
	return i, err
}

const countMFARecoveryUsers = `-- name: CountMFARecoveryUsers :one
SELECT COUNT(DISTINCT c.user_id)
FROM auth.mfa_recovery_codes c
JOIN "user".users u ON u.id = c.user_id
WHERE u.tenant_id = $1 AND u.deleted_at IS NULL AND c.used_at IS NULL
`

func (q *Queries) CountMFARecoveryUsers(ctx context.Context, tid pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countMFARecoveryUsers, tid)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countMFATotpUsers = `-- name: CountMFATotpUsers :one
SELECT COUNT(*)
FROM auth.mfa_totp t
JOIN "user".users u ON u.id = t.user_id
WHERE u.tenant_id = $1 AND u.deleted_at IS NULL AND t.confirmed_at IS NOT NULL
`

func (q *Queries) CountMFATotpUsers(ctx context.Context, tid pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countMFATotpUsers, tid)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countStickinessPriorWeek = `-- name: CountStickinessPriorWeek :one
SELECT
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= NOW() - INTERVAL '8 days'
                                      AND created_at <  NOW() - INTERVAL '7 days') AS dau_prev_week,
    COUNT(DISTINCT user_id) FILTER (WHERE created_at >= NOW() - INTERVAL '37 days'
                                      AND created_at <  NOW() - INTERVAL '7 days') AS mau_prev_week
FROM auth.sessions
WHERE tenant_id = $1
`

type CountStickinessPriorWeekRow struct {
	DauPrevWeek int64
	MauPrevWeek int64
}

func (q *Queries) CountStickinessPriorWeek(ctx context.Context, tid pgtype.UUID) (CountStickinessPriorWeekRow, error) {
	row := q.db.QueryRow(ctx, countStickinessPriorWeek, tid)
	var i CountStickinessPriorWeekRow
	err := row.Scan(&i.DauPrevWeek, &i.MauPrevWeek)
	return i, err
}

const countTotalUsersWindows = `-- name: CountTotalUsersWindows :one
SELECT
    COUNT(*) FILTER (WHERE deleted_at IS NULL) AS users_now,
    COUNT(*) FILTER (WHERE deleted_at IS NULL AND created_at <= NOW() - INTERVAL '30 days') AS users_prev
FROM "user".users
WHERE tenant_id = $1
`

type CountTotalUsersWindowsRow struct {
	UsersNow  int64
	UsersPrev int64
}

func (q *Queries) CountTotalUsersWindows(ctx context.Context, tid pgtype.UUID) (CountTotalUsersWindowsRow, error) {
	row := q.db.QueryRow(ctx, countTotalUsersWindows, tid)
	var i CountTotalUsersWindowsRow
	err := row.Scan(&i.UsersNow, &i.UsersPrev)
	return i, err
}

const getFailedLoginsHourly24h = `-- name: GetFailedLoginsHourly24h :many
WITH hours AS (
    SELECT date_trunc('hour', h) AS hour
    FROM generate_series(
        date_trunc('hour', NOW() - INTERVAL '23 hours'),
        date_trunc('hour', NOW()),
        '1 hour'::interval
    ) AS h
),
grouped AS (
    SELECT date_trunc('hour', created_at) AS hour, COUNT(*) AS n
    FROM audit.events
    WHERE tenant_id = $1
      AND action = 'auth.login_failed'
      AND created_at >= NOW() - INTERVAL '24 hours'
    GROUP BY 1
)
SELECT to_char(hours.hour, 'HH24:MI') AS hour, COALESCE(grouped.n, 0) AS attempts
FROM hours LEFT JOIN grouped ON grouped.hour = hours.hour
ORDER BY hours.hour ASC
`

type GetFailedLoginsHourly24hRow struct {
	Hour     string
	Attempts int64
}

func (q *Queries) GetFailedLoginsHourly24h(ctx context.Context, tid pgtype.UUID) ([]GetFailedLoginsHourly24hRow, error) {
	rows, err := q.db.Query(ctx, getFailedLoginsHourly24h, tid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetFailedLoginsHourly24hRow{}
	for rows.Next() {
		var i GetFailedLoginsHourly24hRow
		if err := rows.Scan(&i.Hour, &i.Attempts); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getLoginActivity14d = `-- name: GetLoginActivity14d :many
WITH days AS (
    SELECT date_trunc('day', d)::date AS day
    FROM generate_series(
        date_trunc('day', NOW() - INTERVAL '13 days'),
        date_trunc('day', NOW()),
        '1 day'::interval
    ) AS d
),
grouped AS (
    SELECT date_trunc('day', created_at)::date AS day,
           COALESCE(metadata->>'method', 'password') AS method,
           COUNT(*) AS n
    FROM audit.events
    WHERE tenant_id = $1
      AND action = 'auth.login_succeeded'
      AND created_at >= NOW() - INTERVAL '14 days'
    GROUP BY 1, 2
)
SELECT
    days.day::text AS day,
    COALESCE(SUM(n) FILTER (WHERE method = 'password'),     0)::bigint AS password,
    COALESCE(SUM(n) FILTER (WHERE method = 'passkey'),      0)::bigint AS passkey,
    COALESCE(SUM(n) FILTER (WHERE method = 'social'),       0)::bigint AS social,
    COALESCE(SUM(n) FILTER (WHERE method = 'saml'),         0)::bigint AS saml,
    COALESCE(SUM(n) FILTER (WHERE method = 'oidc'),         0)::bigint AS oidc
FROM days LEFT JOIN grouped ON grouped.day = days.day
GROUP BY days.day
ORDER BY days.day ASC
`

type GetLoginActivity14dRow struct {
	Day      string
	Password int64
	Passkey  int64
	Social   int64
	Saml     int64
	Oidc     int64
}

func (q *Queries) GetLoginActivity14d(ctx context.Context, tid pgtype.UUID) ([]GetLoginActivity14dRow, error) {
	rows, err := q.db.Query(ctx, getLoginActivity14d, tid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetLoginActivity14dRow{}
	for rows.Next() {
		var i GetLoginActivity14dRow
		if err := rows.Scan(
			&i.Day,
			&i.Password,
			&i.Passkey,
			&i.Social,
			&i.Saml,
			&i.Oidc,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getWeeklyActivity8w = `-- name: GetWeeklyActivity8w :many
WITH weeks AS (
    SELECT date_trunc('week', d) AS week_start
    FROM generate_series(
        date_trunc('week', NOW() - INTERVAL '7 weeks'),
        date_trunc('week', NOW()),
        '1 week'::interval
    ) AS d
),
w AS (
    SELECT
        date_trunc('week', created_at) AS week_start,
        COUNT(DISTINCT user_id) AS wau
    FROM auth.sessions
    WHERE sessions.tenant_id = $1 AND created_at >= date_trunc('week', NOW() - INTERVAL '7 weeks')
    GROUP BY 1
),
d AS (
    SELECT
        lat.dw AS week_start,
        AVG(daily_users)::bigint AS dau_avg
    FROM (
        SELECT
            date_trunc('day', created_at) AS day,
            COUNT(DISTINCT user_id) AS daily_users
        FROM auth.sessions
        WHERE tenant_id = $1 AND created_at >= date_trunc('week', NOW() - INTERVAL '7 weeks')
        GROUP BY 1
    ) daily, LATERAL (SELECT date_trunc('week', daily.day) AS dw) lat
    GROUP BY 1
)
SELECT
    to_char(weeks.week_start, '"W"IW') AS week,
    COALESCE(w.wau, 0) AS wau,
    COALESCE(d.dau_avg, 0) AS dau
FROM weeks
LEFT JOIN w ON w.week_start = weeks.week_start
LEFT JOIN d ON d.week_start = weeks.week_start
ORDER BY weeks.week_start ASC
`

type GetWeeklyActivity8wRow struct {
	Week string
	Wau  int64
	Dau  int64
}

func (q *Queries) GetWeeklyActivity8w(ctx context.Context, tid pgtype.UUID) ([]GetWeeklyActivity8wRow, error) {
	rows, err := q.db.Query(ctx, getWeeklyActivity8w, tid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetWeeklyActivity8wRow{}
	for rows.Next() {
		var i GetWeeklyActivity8wRow
		if err := rows.Scan(&i.Week, &i.Wau, &i.Dau); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
