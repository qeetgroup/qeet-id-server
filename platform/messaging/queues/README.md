# platform/messaging/queues

Generic async job queue interfaces and implementations.

Planned: in-process work queue, database-backed queue (using the outbox pattern already in
`platform/events/outbox`), priority queue.
