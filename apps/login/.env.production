# Production build-time config for the hosted login (qeetid-login).
# Next inlines NEXT_PUBLIC_* at build; only public values belong here (no secrets).

# Live backend API (the app appends /v1/... itself).
NEXT_PUBLIC_API_URL=https://id.api.qeet.in
