apiVersion: v2
name: qeet-id
description: Qeet ID — identity platform (Go modular monolith)
type: application
# Chart version: bump on chart changes (SemVer, independent of the app).
version: 0.1.0
# Default app version; release CI overrides with --set image.tag / appVersion.
appVersion: "0.1.0"
home: https://qeet.in
sources:
  - https://github.com/qeetgroup/qeet-id
maintainers:
  - name: Qeet Group
keywords:
  - identity
  - oidc
  - saml
  - scim
